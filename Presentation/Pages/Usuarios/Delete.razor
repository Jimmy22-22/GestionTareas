@page "/usuarios/delete/{Id:int}"
@inject UsuarioService UsuarioService
@inject NavigationManager Navigation

<h3 class="titulo-eliminar">
    <i class="bi bi-trash3-fill icono-titulo"></i> Eliminar Usuario
</h3>

<p class="mensaje-confirmacion">¿Estás seguro que deseas eliminar este usuario?</p>

<div class="botones-accion">
    <button class="btn-eliminar me-2" @onclick="Eliminar">
        <i class="bi bi-trash-fill icono-boton"></i> Sí, eliminar
    </button>

    <button class="btn-cancelar" @onclick="Cancelar">
        <i class="bi bi-x-circle icono-boton"></i> Cancelar
    </button>
</div>

<style>
    .titulo-eliminar {
        text-align: center;
        font-size: 1.5rem;
        font-weight: bold;
        color: #dc3545;
        margin-bottom: 1.5rem;
    }

    .icono-titulo {
        margin-right: 0.5rem;
    }

    .mensaje-confirmacion {
        text-align: center;
        font-size: 1rem;
        color: #555;
        margin-bottom: 2rem;
    }

    .botones-accion {
        display: flex;
        justify-content: center;
        gap: 1rem;
    }

    .btn-eliminar {
        padding: 0.75rem 1.5rem;
        background-color: #dc3545;
        color: #fff;
        font-size: 1rem;
        font-weight: bold;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        transition: background-color 0.3s ease;
    }

    .btn-eliminar:hover {
        background-color: #c82333;
    }

    .btn-cancelar {
        padding: 0.75rem 1.5rem;
        background-color: #6c757d;
        color: #fff;
        font-size: 1rem;
        font-weight: bold;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        transition: background-color 0.3s ease;
    }

    .btn-cancelar:hover {
        background-color: #5a6268;
    }

    .icono-boton {
        margin-right: 0.5rem;
    }
</style>

@code {
    [Parameter] public int Id { get; set; }

    private async Task Eliminar()
    {
        await UsuarioService.Eliminar(Id);
        Navigation.NavigateTo("/usuarios");
    }

    private void Cancelar()
    {
        Navigation.NavigateTo("/usuarios");
    }
}
